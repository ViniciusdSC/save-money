import styled from 'styled-components';
import {Button as NativeButton} from 'react-native-paper';

export const TextInput = styled.TextInput`
  margin: 10px;
  font-size: 25px;
`;

export const Title = styled.Text`
  margin: 10px;
  font-size: 32px;
`;

export const Submit = styled(NativeButton)`
  margin: 10px;
`;
